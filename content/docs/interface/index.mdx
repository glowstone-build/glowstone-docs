---
title: "Workspace & Docking"
description: "Arrange, resize, hide and restore panels in the dockable workspace, with a reference for what each panel does."
---

import { LayoutPanelLeft, ListChecks, SlidersHorizontal, RotateCcw, Settings2, Monitor } from 'lucide-react';

Glowstone arranges everything around a single dockable workspace: a tree of split panes with the 3D Viewport at the centre. Drag borders to resize, drag tabs to regroup, toggle panels on and off.

<ImagePlaceholder
  label="Screenshot"
  ratio="16 / 9"
  caption="A rearranged Glowstone workspace: Scene panel docked left, Inspector docked right, 3D Viewport in the centre showing a lit rig, and a bottom data strip with the Fixtures (Patch), DMX and Cues tabs grouped together. Viewport header bar visible along the top of the centre pane."
/>

## How docking works

The workspace is a **tree-split** layout, not a floating-window manager. Every panel is a pane in a recursively split tree, and the **Viewport** is the always-present anchor that the other panels dock around.

- **Resize:** drag the border between two panels to change their split ratio.
- **Regroup:** drag a panel's tab onto another pane to move it there, or reorder tabs within a pane by dragging.
- **The Viewport never closes.** Every other panel can be hidden; the Viewport is the fixed focal point.

<Callout type="info" title="No floating panels">
Panels always dock. You cannot tear a panel off into its own OS window — the layout is a single split tree inside the main Glowstone window.
</Callout>

<Callout type="info" title="Tab title vs. panel name">
A few panels show a tab label that differs from their function. The **Patch** panel is labelled **Fixtures** in the dock, and the **DMX Monitor** panel is labelled **DMX**. The reference below uses the functional name and notes the tab title for each.
</Callout>

## Panels and their canonical edges

When you reopen a hidden panel, it returns to its **canonical edge** as a full-height or full-width pane, rather than wherever it last sat. This keeps the layout predictable — toggling the Inspector, for example, always brings it back as the right sidebar instead of merging it into the Viewport tab strip.

| Panel | Tab title | Reopen edge | Default size |
| --- | --- | --- | --- |
| Viewport | Viewport | Centre (always open) | — |
| Scene | Scene | Left | 17% width |
| Library | Library | Left | 17% width |
| Inspector | Inspector | Right | ~20% width |
| DMX Monitor | DMX | Bottom | 30% height |
| Patch | Fixtures | Bottom | 30% height |
| Cues | Cues | Bottom | 30% height |
| Connectivity | Connectivity | Bottom | 30% height |
| Render | Render | Focused leaf (no canonical edge) | — |

The **Viewport** and **Render** have no canonical edge — they reopen as tabs in the focused leaf (usually the Viewport tab strip).

## Showing and hiding panels

Toggle any panel from the **Window › Panels** checkbox list (Render is included). The Viewport is excluded — it is always open.

Two panels also have keyboard toggles in the Viewport context:

| Shortcut | Action |
| --- | --- |
| `N` | Toggle the Inspector (re-opens on the right edge) |
| `T` | Toggle the T-panel tool rail (left of the Viewport) |

<Callout type="idea">
Press `N` to reclaim the full width of the Viewport for a clean preview, then press it again to bring the Inspector back exactly where it was.
</Callout>

## Resetting the layout

If live dragging has left the workspace messy, choose **Window › Reset Layout**. This re-applies the active workspace's saved dock geometry — it reverts your manual splits and resizes, but leaves the active tool and overlay state untouched.

<Callout type="info">
Reset Layout restores the *current* workspace's saved arrangement, not a global default. To switch to a different saved arrangement, use a [Workspace](/docs/interface/workspaces).
</Callout>

## What persists

The dock arrangement persists within a session as live UI state, and across sessions only when you save it as a [Workspace](/docs/interface/workspaces). A few panel details are deliberately session-only.

| Persists | Session-only |
| --- | --- |
| Dock layout saved as a workspace (`workspaces.json`) | Scene outliner expanded/collapsed nodes |
| Inspector category open/closed state (`inspector.json`) | Scene filter and search text |
| Library Recents + Favourites (`library.json`) | Tab positions within panels (revert to the saved workspace on close) |
| Keymap overrides (`keymap.json`) | |
| Camera bookmarks, 9 slots (`bookmarks.json`) | |

<Callout type="warn">
General preferences are applied live but may not all persist between sessions yet. The keymap and camera bookmarks listed above *do* persist. See [Preferences & Theme](/docs/interface/preferences).
</Callout>

## The Viewport header bar

Only the Viewport tab draws a header bar. Every other panel uses its dock tab as its sole label. The header holds the controls you reach for most while working in 3D.

<TypeTable
  type={{
    'Display Mode': { description: 'Segmented buttons that switch the Viewport shading in real time.', type: 'Beauty / Unlit / Wireframe', default: 'Beauty' },
    'Exposure': { description: 'Viewport exposure multiplier for brightness control. Does not change the scene, only the preview. Labelled "Exp" in the header.', type: '0.05–8.0', default: '1.0' },
    'Grid': { description: 'Toggle the origin grid and world axes.', type: 'Checkbox', default: 'On' },
    'Beam': { description: 'Toggle beam gizmo wireframes (volumetric cone visualisation).', type: 'Checkbox', default: 'Off' },
    'Step': { description: 'Per-type snap increments for Move (m), Rotate (°) and Scale (×). Enabled only when Snap is on.', type: 'Menu', default: '—' },
    'Snap mode': { description: 'What Move snapping targets. Enabled only when Snap is on.', type: 'Grid / Vertex / Surface', default: 'Grid' },
    'Snap': { description: 'Turn grid/increment snapping on or off. Holding Ctrl mid-drag inverts it.', type: 'Checkbox', default: 'Off' },
    'Pivot Point': { description: 'Sets the pivot used by rotate and scale.', type: 'Median Point / Active Element / Individual Origins / 3D Cursor', default: 'Median Point' },
    'Transform Orientation': { description: 'Which axes the transform gizmo and keyboard locks follow.', type: 'Global / Local / View', default: 'Global' },
    'T-panel': { description: 'Show or hide the left tool rail (also the T key).', type: 'Toggle', default: '—' },
    'Fullscreen': { description: 'Toggle OS window fullscreen (also F11).', type: 'Toggle', default: 'Off' },
  }}
/>

<ImagePlaceholder
  label="Close-up"
  ratio="21 / 9"
  caption="Close-up of the 26px Viewport header bar, left to right: Display Mode segmented buttons (Beauty/Unlit/Wireframe), Exposure (Exp) drag value, Grid checkbox, Beam checkbox, Step menu, Snap mode dropdown, Snap toggle, Pivot Point dropdown, Transform Orientation dropdown, T-panel toggle, and Fullscreen button far right."
/>

<Callout type="info">
Exposure here only affects the live preview brightness. For final image tone mapping and colour, see [Look & Colour Management](/docs/rendering/look).
</Callout>

## What each panel does

Each panel below shows what it displays, what you do in it, and the deep page that covers it in full.

### Viewport

The 3D stage preview and the immovable anchor of every layout — all other panels dock around it. It renders the live scene with DMX-driven lighting, volumetric beams in haze, and fixture optical output in real time. The Viewport has its own [header bar](#the-viewport-header-bar) (display mode, exposure, grid, snap, pivot, transform orientation), and it is always present — it never appears in the **Window › Panels** toggle list.

See [Camera & Navigation](/docs/navigation), [Views, Gizmo & Pie Menus](/docs/navigation), and [Display Modes & Overlays](/docs/navigation/overlays).

### Scene

Tab title: **Scene**. A custom recursive outliner of the project hierarchy under one **Scene** root: **World** (sky/HDRI) with **Environment** fog volumes nested beneath it, then **Fixtures**, **Objects** (static geometry), **Screens** (LED content), and **Pyro** devices. Use it to select, rename (double-click), toggle visibility (the eye column), and build selections. A name filter and type/state filter chips narrow the tree, and a Sort control orders fixtures by **Patch**, **Name**, **Type**, or **Seq** (sequence number).

Fixture rows show the **sequence number** (the lighting-console "channel" id) leading the name, plus a right-aligned patch (`universe.address`) chip and channel footprint.

<Callout type="info">
The outliner's expand/collapse state, filter chips, and search text are session state held in memory — they survive closing and reopening the panel within a run, but are not persisted to disk.
</Callout>

See [The Scene Outliner](/docs/scene/outliner) and [Selecting Objects](/docs/scene/selection).

#### Type & state filter chips

The chip row narrows the tree. The **type** chips are mutually exclusive: **All, Fixtures, Objects, Screens, Pyro** (selecting one restricts the tree to that kind; World/Environment only show under **All**). The **state** chips are composable toggles that constrain fixtures only: **Unpatched**, **Selected**, **Conflicts**. Activating any state chip puts the tree into fixture-focus mode and hides the non-fixture kinds.

### Library

Tab title: **Library**. The content browser — a searchable, sortable, multi-select list of fixture / laser / screen / environment templates, with **Recent** and **Favourites** sections pinned at the top when not searching. Sort by **Category**, **Name**, or **Maker**, and narrow by content-class chip (**All / Fixtures / Lasers / Screens / Environments / Imported**). Single-click selects; `Cmd`/`Ctrl`+click toggles; `Shift`+click range-selects. Double-click adds an entry, drag one into the Viewport to place it, or select and press `Enter`. The header toolbar hosts **Online** (GDTF Share), **Export** (MVR), **Import MVR**, and **Import GDTF**.

<Callout type="info">
Pinned Recents and Favourites persist between sessions (stored in `library.json` in the [config directory](/docs/projects#where-glowstone-stores-files)).
</Callout>

See [The Fixture Library](/docs/library) and [Importing GDTF Fixtures](/docs/library/import-gdtf).

### Inspector

Tab title: **Inspector**. Glowstone's unified property editor. The Inspector is selection-aware: it shows different controls depending on what is selected, and edits apply live with immediate feedback in the Viewport.

<ImagePlaceholder
  label="UI panel"
  ratio="4 / 3"
  caption="The Inspector docked on the right edge with a GDTF moving head selected, showing collapsible categories, the property filter box at the top, and the 'Modified only' and 'Pin' header toggles."
/>

#### What the Inspector shows

The displayed editor follows the active selection:

| Selection | Editor shown |
| --- | --- |
| Fixture (built-in) | Heading + provenance chip, Transform, and the Fixture optics/level/beam grid |
| Fixture (GDTF) | Thumbnail, beam spec, emitter/cell summary, Transform + Fixture grid, Optics, Wheels, and a DMX-modes reference table |
| Geometry (Object) | Name, visibility, world transform (position / rotation / uniform scale), size note |
| Screen | Name, transform, and the LED-screen content/layout controls |
| Pyro | Heading, visibility, transform, and the kind-aware Effect/Movement/Quality/DMX grid |
| Environment | Transform (Center, Size W/H/D), Density, Tint, and advanced scattering knobs |
| World (Sky) | Render Properties + the shared World controls (HDRI / ambient / fog) |

Pyro devices are a placed entity with their own inline DMX patch — see [Pyro Devices](/docs/fixtures/pyro).

#### Filtering and categories

- **Property filter** — type in the filter box to hide rows whose labels don't fuzzy-match; a category with no matching row hides entirely. The filter is per-session (not saved).
- **Modified only** — when on, hides any property still at its default value, so you see only what you've changed. Per-session (not saved).
- **Pin** — lock the Inspector to the currently-selected single fixture so it stays put as the Viewport/outliner selection changes. Per-session.
- **Collapsible categories** — click a section header to fold or unfold it. Each category's open/closed state persists across sessions (saved to `inspector.json`).

See [Beam Parameter Reference](/docs/fixtures/beam-engine) and [Transforming](/docs/scene/transforming).

### DMX Monitor

Tab title: **DMX**. A live 512-channel universe grid (16 × 32 cells) — one cell per channel — showing each channel's live level (as a percentage bar; the raw `0`–`255` value appears in the cell tooltip) and its patch occupant (the fixture + attribute mapped to that address, tinted by a per-fixture identity colour). A universe selector dropdown (with prev/next) switches which universe you're viewing, conflicts ring in red, and a live/idle indicator with source count tracks incoming DMX. Clicking an occupied cell selects its fixture.

See [The DMX Monitor](/docs/dmx/dmx-monitor) and [DMX Overview](/docs/dmx).

### Patch (Fixtures)

Tab title: **Fixtures**. The Fixture Manager — a data-dense, sortable, filterable table of every fixture with multi-select synced to the 3D/Inspector selection and bulk patch editing. Columns are **Seq, Fixture, Type, Univ, Addr, Mode, Ch** (channel footprint), and a status cell (conflict / off). Sort by **Patch / Name / Type / Seq**; filter by search, **conflicts-only**, or **unpatched-only**. Editable cells per row: the sequence number, universe, address, and DMX mode. A bulk toolbar (shown when fixtures are selected) patches a range sequentially, sets the universe, enables/disables, and sets a shared mode; **Renumber** reassigns sequence numbers by stage position.

See [Patching Fixtures](/docs/dmx/patching) and [DMX Modes & Channels](/docs/library/profile-editor).

### Cues

Tab title: **Cues**. Saved looks plus crossfade playback. **Record** captures the current look of every fixture (pan/tilt/colour/intensity/beam/optics) as a cue with a fade time. The cue list lets you recall a cue by click (crossfading the continuous fields, snapping the discrete optics), step with **Prev** / **Go**, edit each cue's fade time inline, and delete a cue. A progress bar tracks an in-flight fade.

<Callout type="info">
While a fade is in progress the recalled look overrides live DMX.
</Callout>

See [Cues & Playback](/docs/cues).

### Connectivity

Tab title: **Connectivity**. Art-Net and sACN input settings plus per-source status. A **Receive DMX** toggle starts/stops the listener; below it you set the active protocols (**Art-Net** / **sACN**), the bind interface IP, the sACN universe list, the merge policy, and the Art-Net priority, with a **Reapply** button for protocol/interface changes. A live **Sources** table lists each source (protocol, name, universes, priority, FPS, sequence errors, age).

<Tabs items={['Art-Net', 'sACN']}>
  <Tab value="Art-Net">Art-Net input — broadcast, so every universe is received regardless of the universe list.</Tab>
  <Tab value="sACN">sACN / E1.31 input — the universe list selects which multicast groups to join.</Tab>
</Tabs>

See [Connectivity: Art-Net & sACN](/docs/dmx/connectivity) and [Connecting a Console](/docs/dmx/connectivity).

### Render

Tab title: **Render**. The still-image render output: a toolbar (**Render** / **Re-render** / **Cancel**, **Save to Disk**, **Save Copy**), a progress strip (Sample i/N + elapsed time), and the large converging preview that refines as accumulation passes complete. The render *settings* themselves — output resolution, scale, format, sampling, and performance options — live in **World › Render Properties** (select World, then the Inspector); the Render tab drives and displays the result.

<Callout type="warn">
Some render-related features are visible but not yet functional: the **Animation** render button (greyed), **Motion blur**, **Caustics**, and the **"New Window"** render-display option. The froxel volumetric grid is off by default — the per-pixel raymarch is the default and looks better.
</Callout>

See [Rendering Overview](/docs/rendering) and [Rendering a Still Image](/docs/rendering/render-image).

## Common tasks

<Steps>
<Step>
### Resize a panel
Drag the border between two panels until the split ratio looks right.
</Step>
<Step>
### Regroup tabs
Drag a panel's tab onto another pane to move it, or reorder tabs within a pane.
</Step>
<Step>
### Hide a panel
Uncheck it in **Window › Panels**, or press `N` (Inspector) / `T` (tool rail).
</Step>
<Step>
### Restore the layout
Choose **Window › Reset Layout** to snap back to the active workspace's saved arrangement.
</Step>
</Steps>

<Accordions type="single">
  <Accordion title="Why did my reopened panel ignore where it was before?">
    Toggled panels reopen at their canonical edge — Scene/Library on the left, Inspector on the right, data panels at the bottom. This is intentional, so a quick toggle never merges a panel into the wrong pane. Save a [Workspace](/docs/interface/workspaces) to lock in a custom arrangement.
  </Accordion>
  <Accordion title="Can I float a panel in its own window?">
    No. Glowstone uses a single tree-split dock inside one window. Panels always dock; there is no floating mode.
  </Accordion>
  <Accordion title="My tab order keeps resetting — why?">
    Tab positions within a panel are session-only. They revert to the saved workspace when you close it. Save a workspace to make an arrangement stick across sessions.
  </Accordion>
</Accordions>

## See also

<Cards>
  <Card icon={<ListChecks />} title="Workspaces" href="/docs/interface/workspaces">
    Save and switch arrangement presets, tools and overlays.
  </Card>
  <Card icon={<Settings2 />} title="Preferences & Theme" href="/docs/interface/preferences">
    Live preferences, theme and what persists between sessions.
  </Card>
  <Card icon={<SlidersHorizontal />} title="Views, Gizmo & Pie Menus" href="/docs/navigation">
    Pivot, orientation, snapping and the Viewport pie menus.
  </Card>
  <Card icon={<Monitor />} title="Interface Overview" href="/docs/getting-started/interface-overview">
    A guided tour of the Glowstone window and its regions.
  </Card>
</Cards>
